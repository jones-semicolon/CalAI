name: Build Cal AI (Android & iOS)

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allows you to trigger this manually from the Actions tab

jobs:
  build-android-debug:
    name: Build Debug APK
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: "17"

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable" # Gets the latest stable release automatically
          cache: true
          # Removed "flutter-version: 'stable'" to fix the error

      - name: Install Dependencies
        run: flutter pub get
      - name: Decode Keystore and Firebase Config
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          GOOGLE_SERVICES_JSON: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: |
          cd android
          # Decode the Keystore
          echo $KEYSTORE_BASE64 | base64 --decode > app/upload-keystore.jks

          # Decode the Google Services JSON
          echo $GOOGLE_SERVICES_JSON | base64 --decode > app/google-services.json

          # Recreate key.properties
          cat << EOF > key.properties
          storePassword=$STORE_PASSWORD
          keyPassword=$KEY_PASSWORD
          keyAlias=$KEY_ALIAS
          storeFile=../app/upload-keystore.jks
          EOF

      - name: Print Release SHA1
        run: |
          keytool -list -v \
          -alias ${{ secrets.KEY_ALIAS }} \
          -keystore android/app/upload-keystore.jks \
          -storepass ${{ secrets.STORE_PASSWORD }}

      - name: Build Android APK (Release)
        run: flutter build apk --release

      - name: Upload Android Artifact
        uses: actions/upload-artifact@v4
        with:
          name: cal-ai-android-release
          path: build/app/outputs/flutter-apk/app-release.apk

      - name: Create Release & Upload APK
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: build/app/outputs/flutter-apk/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ---------------------------------------------------
  # JOB 2: Build Unsigned iOS IPA
  # ---------------------------------------------------
  build_ios:
    name: Build iOS IPA (Unsigned)
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"

      - name: Install dependencies
        run: flutter pub get

      - name: Build unsigned iOS app
        # The --no-codesign flag skips Apple's mandatory signing step
        run: flutter build ios --release --no-codesign

      - name: Package into IPA
        run: |
          cd build/ios/iphoneos
          mkdir Payload
          cp -r Runner.app Payload/
          zip -r cal_ai_unsigned.ipa Payload

      - name: Upload iOS Artifact
        uses: actions/upload-artifact@v4
        with:
          name: cal-ai-ios-release
          path: build/ios/iphoneos/cal_ai_unsigned.ipa
